import 'package:flutter/material.dart';

class TaspehScreen extends StatelessWidget {
  const TaspehScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
